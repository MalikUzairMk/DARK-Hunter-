# Lock-Termux

```
apt update
apt upgrade
apt install python2
apt install bash
apt install git
git clone https://github.com/MalikUzairMk/DARK-Hunter-
cd Lock-Termux
bash setup.sh
set you password
```
